<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.library.ui">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Library"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".activity.AddEditBookRent"></activity>
        <activity android:name=".activity.AddEditNamedEntity" />
        <activity android:name=".activity.AuthorListActivity" />
        <activity android:name=".activity.AddEditAuthor" />
        <activity android:name=".activity.BookListActivity" />
        <activity android:name=".activity.AddEditBook" />
        <activity android:name=".activity.UserListActivity" />
        <activity android:name=".activity.AddEditUser" />
        <activity android:name=".activity.ReaderMenu" />
        <activity android:name=".activity.OperatorMenu" />
        <activity android:name=".activity.AdminMenuActivity" />
        <activity android:name=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>